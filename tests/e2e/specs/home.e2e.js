describe('Dashboard Page', () => {
  it('has the correct title and heading', () => {
    cy.visit('/')
    // cy.title().should('equal', 'Vuetify Dashboard' Dashboard')
    // cy.contains('h1', 'Dashboard Page')
  })
})
